# Selenium Cucumber Test Automation Framework

This project demonstrates an automated UI testing framework using **Selenium WebDriver** with **Cucumber** for Behavior-Driven Development (BDD). It supports clear Gherkin-based test cases and clean Page Object Model (POM) structure for maintainable tests.

---

## 🧰 Tech Stack

- **Language**: Java(Above 11)
- **BDD Tool**: Cucumber
- **Automation Tool**: Selenium WebDriver
- **Build Tool**: Maven 
- **Test Runner**: JUnit 
- **Design Pattern**: Page Object Model (POM)
- **Reporting**: Cucumber HTML reports 
- **IDE**: IntelliJ IDEA / Eclipse

---

src
└── test
    └── java
        ├── hooks         # hooks (Before/After scenarios)
        ├── pages         # Page Object Model classes for each application screen/page
        ├── runner        # Test runner classes
        ├── steps         # Step definition classes used in BDD scenarios
        └── utils         # Utility classes (driver setup, config)
├── test
│   └── resources
│       └── features          # .feature files (BDD scenarios)

## 🚀 Getting Started
Prerequisites:
Install Java 11 or above
Install Maven 3.9.5 or above

Steps to run tests;
1. Unzip the folder
2. Build project/Install Dependencies - mvn clean install
3. Run mvn test/or you can run the TestRunner.java class